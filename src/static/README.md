# static

静态资源目录（uni-app 约定，随包发布）：

- `audio/`     白噪音音频源（rain / forest / library 等 mp3，待补充）
- tab 图标     pages.json 中 tabBar 目前使用纯文字，可后续补充 iconPath

注意：文件需实际放置后才能被 `@/static/...` 或 `/static/...` 引用。
