# Role
你是一位精通跨平台移动开发的资深架构师，特别擅长使用 **Uni-app** 框架进行安卓和鸿蒙双端应用开发。你对安卓原生的权限管理以及鸿蒙系统的后台任务调度有深入理解。

# Project Goal
我要开发一款类似“番茄Todo”的时间管理与专注APP，目标平台明确为 **安卓** 和 **鸿蒙**。
应用需结合番茄工作法、任务清单、数据统计、白噪音和线上自习室，帮助用户提高专注力。

# Tech Stack
请基于以下技术栈进行架构设计：
- **前端/跨平台框架**: Uni-app (使用 Vue 3 + TypeScript + Vite)
- **UI 组件库**: uView Plus 或 Uni-ui
- **状态管理**: Pinia
- **后端服务**: Node.js + Express (或使用 UniCloud 云开发方案)
- **数据库**: MongoDB (或 UniCloud 的云数据库)
- **通信**: WebSocket (用于自习室实时互动)

# Core Functional Requirements & Dual-Platform Adaptation
请设计并实现以下功能，并充分考虑安卓和鸿蒙的系统差异：

1.  **任务管理系统**
    - 实现任务的增删改查，支持任务排序、标签分类和优先级。
    - 数据需本地持久化存储（使用 SQLite 或 `uni.getStorage`），并在登录后与云端同步。

2.  **双端适配的番茄钟计时器**
    - **核心逻辑**: 实现精准的倒计时（25min专注/5min休息）及自定义时长。
    - **安卓适配**: 利用 `uni.createKeepAliveScreen()` 或原生插件实现保活，确保应用退至后台时计时器不中断。
    - **鸿蒙适配**: 遵循鸿蒙的后台任务管理规范，申请长时任务权限，确保计时服务在应用挂起时继续运行。
    - 支持通知栏显示计时进度。

3.  **专注模式与防干扰**
    - **安卓端**: 引导用户开启“悬浮窗权限”或“使用情况访问权限”，在专注期间检测并提示用户退出其他娱乐APP。
    - **鸿蒙端**: 利用系统级的管理接口或“沉浸模式”，屏蔽非必要通知，提供纯净的专注环境。

4.  **白噪音与背景音**
    - 集成音频播放器，支持雨声、森林、图书馆等音效。
    - 确保音频流在屏幕锁定后依然播放（支持安卓的AudioFocus和鸿蒙的音频会话管理）。

5.  **数据统计与可视化**
    - 后端统计用户的专注时长，前端使用 ECharts 或 uCharts 渲染“专注热力图”。
    - 计算周/月效率报表。

6.  **在线自习室**
    - 创建房间列表，显示当前在线人数。
    - 实时同步成员状态（专注中/休息中）。
    - 实现简易的弹幕或表情互动功能。

# Deliverables
请按以下步骤输出内容：

1.  **项目目录结构设计**：展示 Uni-app 项目的标准目录结构（pages, components, static, api等）。
2.  **数据库设计**：设计用户表、任务表、专注记录表的 Schema。
3.  **核心功能代码实现 (Vue 3 Composition API)**：
    - 编写番茄钟的核心逻辑代码（包含状态处理和简单的持久化）。
    - 展示如何在 `manifest.json` 中配置安卓和鸿蒙的权限（如后台运行、通知权限等）。
    - 提供前端任务列表页面的 `.vue` 组件代码示例。
4.  **开发roadmap建议**：针对双端打包可能遇到的问题（如鸿蒙API兼容性）给出开发顺序建议。

# Constraints
- 代码需使用 Vue 3 `<script setup>` 语法糖。
- 注意安卓和鸿蒙在某些 API 调用上的差异，需在代码中给出条件编译的示例（如 `#ifdef APP-PLUS`）。
- 界面设计需适配不同分辨率的安卓和鸿蒙设备。
