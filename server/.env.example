# 服务端口
PORT=3000

# MongoDB 连接串（本地默认）
MONGODB_URI=mongodb://127.0.0.1:27017/tomato

# JWT 签名密钥（生产务必替换为随机长串）
JWT_SECRET=change-me-to-a-long-random-secret

# 专注记录按日聚合时使用的时区偏移（分钟，东八区 +480）
TZ_OFFSET_MINUTES=480
